import crypto from "node:crypto";
const API=process.env.PHEMEX_API_URL||"https://api.phemex.com";
export function authStatus(){return {credentialsPresent:Boolean(process.env.PHEMEX_API_KEY&&process.env.PHEMEX_API_SECRET),mode:(process.env.TRADING_MODE||"paper").toLowerCase(),apiHost:new URL(API).host};}
export function sign(path,query="",expiry=Math.floor(Date.now()/1000)+60,body=""){const key=process.env.PHEMEX_API_KEY,secret=process.env.PHEMEX_API_SECRET;if(!key||!secret)throw new Error("Phemex credentials are not configured");const sig=crypto.createHmac("sha256",secret).update(path+query+expiry+body).digest("hex");return {"x-phemex-access-token":key,"x-phemex-request-expiry":String(expiry),"x-phemex-request-signature":sig};}
export async function privateRequest(method,path,{query="",body=null}={}){if((process.env.TRADING_MODE||"paper").toLowerCase()==="paper")throw new Error("Private live execution disabled while TRADING_MODE=paper");const text=body==null?"":JSON.stringify(body),headers={...sign(path,query,undefined,text),"content-type":"application/json"};const r=await fetch(`${API}${path}${query?`?${query}`:""}`,{method,headers,body:text||undefined});const j=await r.json().catch(()=>({}));if(!r.ok||j?.code&&j.code!==0)throw new Error(`Phemex request failed (${r.status}/${j?.code??"unknown"})`);return j;}
